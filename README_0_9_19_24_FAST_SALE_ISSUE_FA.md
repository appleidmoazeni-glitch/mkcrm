# MKCRM 0.9.19.24 - Fast Sale Issue on 0.9.19.23

پایه نسخه: `mkcrm_0_9_19_23_INVENTORY_LIVE_REPAIR_SEARCH`

## اصلاح اصلی
- مسیر صدور فاکتور فروش از شماره‌گیری دستی/رزرو شماره و چندین `Invoice/Get` قبل از ثبت حذف شد.
- فاکتور فروش با `InvNo=0` به `Invoice/Put` ارسال می‌شود.
- پاسخ `Invoice/Put` فقط برای `GuId` معتبر استفاده می‌شود و `Result[0].Number=0` شماره فاکتور تلقی نمی‌شود.
- شماره واقعی فاکتور بعد از ثبت با `GuId` و در صورت نیاز با جستجوی فاکتورهای امروز resolve می‌شود.
- چاپ فقط با شماره واقعی `InvNo` فعال می‌شود.

## اصلاح سرعت
- عملیات بعد از صدور، شامل بستن Lead ID، آپدیت مشتری، ثبت تابلو و refresh موجودی، از مسیر پاسخ اصلی خارج شد و به background منتقل شد.
- در `invoiceAuditLogs` زمان مرحله `putMs`، `resolveMs` و `totalBeforeResponseMs` ثبت می‌شود.
- در `saleIssueLocks` وضعیت post-processing ثبت می‌شود.

## موارد حفظ‌شده
- اصلاحات 0.9.19.23 برای live repair موجودی و سرچ بهینه حفظ شده است.
- Auto Sync، تابلو، نقش‌ها، سرچ موجودی، سرچ فاکتور فروش، GeneralRef و افزودنی‌های فاکتور دست‌نخورده باقی ماندند.

## تنظیم اختیاری
- `INVOICE_RESOLVE_MAX_PAGES=40` حداکثر صفحات جستجوی fallback برای resolve شماره واقعی فاکتور.
