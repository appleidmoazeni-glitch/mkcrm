# MKCRM 0.9.19.38 - Sale Snapshot Date8 Raw Fix

## تغییرات
- نسخه 37 نصب نشود اگر هدف تست Sale Snapshot است؛ تاریخ را برای Invoice/Get اشتباه تبدیل می‌کرد.
- تاریخ Sale Snapshot بدون تبدیل شمسی/میلادی و بدون خط تیره به شایگان ارسال می‌شود.
- ورودی‌های معتبر: `14050101` یا `20260701`.
- اگر کاربر `2026-07-01` وارد کند، فقط خط تیره حذف و `20260701` ارسال می‌شود.
- `InvoiceType` از 6 تا 7، `RowCount=20`، و `RowStart` مرحله‌ای حفظ شد.
- اتصال فروش به صندوق/نماینده/Issuer حفظ شد.
- سود تأمین‌کننده همچنان متوقف است.
